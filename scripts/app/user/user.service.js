app.factory('User', function($http, APP_CONSTANTS){
	
	 return {
		 
		 /*
		 // returna a list of all available users
		  users: function(callbackFunction){ 
			$http.get(APP_CONSTANTS.BASE_URL + APP_CONSTANTS.JSON_DIR + 'users.json').success(callbackFunction);
		  },	
		  */	  	  	  
	 };
	
	
});